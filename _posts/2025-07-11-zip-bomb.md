---
title: "💣Nested ZIP Password Chain - CTF Writeup"
image: /assets/image/ms_adc/zipbomb.png
date: 2025-07-11 18:00:00 +0300
categories: [ctfs]
tags: [zip, password, python, automation, scripting, zip-bomb]
layout: post
---

In may 2025, right before the main Microsoft ADC CTF, there was a teaser challenge. In this challenge, we were given a ZIP archive named `ExtractMe.zip`. At first glance, it seemed like a simple password-protected archive — but it turned out to be a **recursive ZIP chain**, where every ZIP file contains **another ZIP file**, and the password for each ZIP is the **name of the next ZIP file inside**, without the `.zip` extension.

---

## 🕵️ Manual Inspection

We began by running `unzip` on the initial file:

```bash
unzip ExtractMe.zip
```

![image](/assets/image/ms_adc/extractme.png)

We were prompted for a password. Upon inspecting the contents of the archive (using tools like `zipinfo`), we found that it contains a single file, for example `44008.zip`. When we tried entering `44008` as the password — it worked! This became the key insight for solving the challenge.

Each ZIP file in the chain followed this pattern:

- `ExtractMe.zip` → contains `44008.zip`, password is `44008`
- `44008.zip` → contains `91521.zip`, password is `91521`
- `91521.zip` → contains `88711.zip`, password is `88711`
- ... and so on.

📸 **The initial unzip prompt.**


![ZIP asking for password](/assets/image/ms_adc/unzip_pass.png)


---

## 🔁 The Problem

The nesting goes on and on — with each layer revealing another ZIP. Doing this manually is not only boring, but very time-consuming. So we decided to automate it using Python.

---

## 🐍 Python Script for Recursive Extraction

Here is the complete Python script we wrote to automate the recursive unzipping process:

```python
import os
import zipfile

def recursive_unzip(start_zip):
    current_zip_path = os.path.abspath(start_zip)
    base_dir = os.path.dirname(current_zip_path)

    count = 0
    while True:
        with zipfile.ZipFile(current_zip_path) as zf:
            # Assume only one file inside
            names = zf.namelist()
            if len(names) != 1:
                print(f"[!] Unexpected contents in {current_zip_path}: {names}")
                break

            inner_file = names[0]
            inner_name = os.path.splitext(os.path.basename(inner_file))[0]

            # Prepare output directory
            next_dir = os.path.join(base_dir, f"extracted_{count}")
            os.makedirs(next_dir, exist_ok=True)

            try:
                print(f"[+] Extracting {current_zip_path} using password: {inner_name}")
                zf.extract(inner_file, path=next_dir, pwd=inner_name.encode())
            except RuntimeError as e:
                print(f"[!] Failed to extract {current_zip_path} with password '{inner_name}': {e}")
                break

            # Move to next ZIP
            current_zip_path = os.path.join(next_dir, inner_file)
            if not zipfile.is_zipfile(current_zip_path):
                print(f"[✓] Extraction complete. Final file: {current_zip_path}")
                break

            count += 1

# Replace with your actual starting ZIP file
if __name__ == "__main__":
    recursive_unzip("ExtractMe.zip")
```

📸 **Running the script.**


![Recursive extraction in progress](/assets/image/ms_adc/running_script.png)


---

## 💻 How to Use the Script

1. Save the script above as `recursive_unzip.py`.
2. Place it in the same directory as `ExtractMe.zip`.
3. Run it:

```bash
python3 recursive_unzip.py
```

4. The script will:
   - Automatically extract each ZIP file using the next filename as the password
   - Store each layer’s extracted content in folders like `extracted_0`, `extracted_1`, etc.
   - Stop when the final (non-ZIP) file is extracted

---

## 📂 Folder Structure During Execution

As the script runs, the directory will look something like this:

```
.
├── ExtractMe.zip
├── recursive_unzip.py
├── extracted_0
│   └── 44008.zip
├── extracted_1
│   └── 91521.zip
├── extracted_2
│   └── 88711.zip
...
```

📸 **folder structure.**


![image](/assets/image/ms_adc/directory_structure.png)


---

## ✅ Final Result

Once the final file is extracted, the script will output a message like:

```
[✓] Extraction complete. Final file: /path/to/extracted_N/flag.txt
```


![image](/assets/image/ms_adc/flag_location.png)

Now you can open that file and retrieve your flag or next challenge step.

📸 **The Final extracted File.**


![Final file extracted](/assets/image/ms_adc/flag.png)


---

## 🧠 Lessons Learned

- Automating repetitive tasks is essential in CTFs.
- Always look for patterns — here, the password logic was very systematic.
- Python’s `zipfile` module is great for handling encrypted ZIPs.

---

## 📎 Resources

- [`recursive_unzip.py`](/assets/image/ms_adc/recursive_unzip.py) – Download the full script here.
- [Python zipfile docs](https://docs.python.org/3/library/zipfile.html)
- [CTF Writeups on this site](/categories/ctfs/)

---

## 💬 Got Questions?

Feel free to reach out or drop a comment below if you got stuck in a similar challenge or want help building custom automation tools!

